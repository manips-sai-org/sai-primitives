set(EXAMPLE_NAME 19-puma_singularity)
# create an executable
ADD_EXECUTABLE (${EXAMPLE_NAME} ${EXAMPLE_NAME}.cpp)
ADD_EXECUTABLE (${EXAMPLE_NAME}_visualization ${EXAMPLE_NAME}_visualization.cpp)

# and link the library against the executable
TARGET_LINK_LIBRARIES (${EXAMPLE_NAME}
	${SAI2-PRIMITIVES_LIBRARIES}
	${SAI2-PRIMITIVES_EXAMPLES_COMMON_LIBRARIES}
	)

TARGET_LINK_LIBRARIES (${EXAMPLE_NAME}_visualization
	${SAI2-PRIMITIVES_LIBRARIES}
	${SAI2-PRIMITIVES_EXAMPLES_COMMON_LIBRARIES}
	)

